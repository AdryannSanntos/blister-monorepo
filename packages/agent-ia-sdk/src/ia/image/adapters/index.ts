/**
 * No image adapter is active yet. Concrete adapters (DALL·E, Flux, Gemini
 * image, …) will be exported from here. Placeholder so the directory and
 * `ia/image` capability are wired and extensible.
 */
export {};
